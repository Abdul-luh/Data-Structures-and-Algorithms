let odd = [1, 3, 4, 5, 7, 9];

// Insert / remove at end - O(1)
// odd.push(11);
// console.log(odd);
// odd.pop();
// console.log(odd);

// Insert / remove at beginning - O(n)
// odd.unshift(11);
// console.log(odd);
// odd.shift();
// console.log(odd);

// Access - O(1)
// console.log(odd[5]);

// Search - O(n)

// Shift / unshift / concat / slice /splice - O(n)
// forEach / map / filter / reduce - O(n)

// NOTE: Avoid Using forEach, map, filter and reduce with loops, because the Big-O notation will become quadratic hence and interv@
